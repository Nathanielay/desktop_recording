# desktop_recording
Build a macOS compatible desktop tool for English learning (Intel and Apple Silicon). The tool runs with user permissions, supports a global hotkey, captures words/phrases/articles from any desktop app 
